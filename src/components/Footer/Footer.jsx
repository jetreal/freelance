import React from 'react'
import styles from './footer.module.sass'
import Atributes from '../center/Atributes/Atributes';

const Footer = (props) => {
    return (
        <div className={styles.footer}>

                       
        </div>
    )
}

export default Footer