import React from 'react'
import styles from './colomn.module.sass'

export default (props) => {
    console.log(props)
    return (
        <div className={props.className}>
           
        </div>
    )
}