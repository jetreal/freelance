import React from 'react'
import styles from './soundRight.module.sass'

const SoundRight = (props) => {
    return (
        <div className={styles.soundRight}>
            <img className={styles.soundRightImg} src={'images/soundLeft.webp'} alt="" />
        </div>
    )
}

export default SoundRight